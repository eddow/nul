set = <library

tuple
={	{} => { {} }
[]	(T,.. Ts) => ( T _,.. set.tuple Ts )
}

sub
={ {} => {}
[] (a,.. r) => (a,.. set.sub r)
[] (_,.. r) => set.sub r 
}

fct
={ (A => B) => {
//TODO
	}
}

id
={ x; x != (p => _!=p) }

/>