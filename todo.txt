1- tokenizer records line/column number
2- syntax exception with cur position / hafl syntax-semantic exception (cannot declare joker, freedom declared twice) ?
3- expression have an origin
4- no more 'compiled' : understand directly : y/n?

- freeEC in described

$append
; append = { ({}, s, s) [] ((a,.. r), s, (a,.. ra ; append(r, s, ra)))}

${:append ({}, s, s) [] ((a,.. r), s, (a,.. ra ; append(r, s, ra)))}


X
; append(X, (4,5,6), (1, 2, 3, 4, 5, 6))
; append={ ({}, s, s) [] ((a,.. r), s, (a,.. ra ; append(r, s, ra)))}
::: donne du flou

codeMirror paste twice Oo (chrome?)

brother["John"]
; brother = { x => y != x; father[x] = father[y]}
; father = { p.name => p.father ; p = .xml["xml/familly.xml"][<person />] }


C:\Users\...\AppData\Local\Google\Chrome\Application\3.0.195.27\Resources\Inspector\Images

{:f f Q x [] }

{:f f x} equivaut f ::() "#set"
g => {:f g f x} ::: sous-set
:::La tautologie devrait etre reperee

o::a { o.a _ }
::: noter l'autoref

.library.file["lib/cmplx.nul"]
.library.file["lib/set.nul"]

set::sigma
 { \/x {x} => {}
[] (a,b,.. o) => ((a=>b),.. set.sigma[b,.. o] )
}
::: devrait noter l'auto-ref

document["#sandBox"](text _)
xml["xml/test.xml"](text _)


x 1
; set[1..3] x
; set
={ {}
[] \/a \/s (a,.. s) => ( set[s] [] (a,.. set[s]) )
}
:::faire! (developper)

todo {
sigma[1..]			//inf decomposition
}

Systeme d'unites !!! cf. lib/units.nul
.library.file["lib/units.nul"]

Pas tout builder hors du browse : trop de .modifiable() apres
