#summary Internal representation of NUL locals.
#labels Phase-Implementation
#sidebar ExpressionClassTree

Internal representation of NulLocal

= Description =

== Components ==
 * [nul_xpr_knowledge#name] `klgRef` : knowledge reference.
 * number `ndx` : Index of the local for the knowledge.
 * string `dbgName` : The name to display for this local. Only if debug.

== Overrides ==

=== sum_dependance ===
Because it has only one dependance it defines.